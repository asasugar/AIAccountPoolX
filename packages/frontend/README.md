# Frontend

前端基于 Vue 3 + TypeScript + Vite，作为 `pnpm workspace` 的一个包存在。

- 开发入口：`pnpm --filter @ai-account-pool-x/frontend dev`
- 构建命令：`pnpm --filter @ai-account-pool-x/frontend build`
- 默认开发端口：`3000`

项目级使用说明见仓库根目录的 [README.md](../../README.md)。
