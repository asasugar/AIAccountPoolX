{
  "name": "@ai-account-pool-x/backend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "python3 -m uvicorn app.main:app --host 0.0.0.0 --port 1455 --reload",
    "start": "python3 -m uvicorn app.main:app --host 0.0.0.0 --port 1455",
    "install:deps": "python3 -m pip install -r requirements.txt"
  }
}
