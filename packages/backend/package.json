{
  "name": "@ai-account-pool-x/backend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "uvicorn app.main:app --host 0.0.0.0 --port 1455 --reload",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 1455",
    "install:deps": "pip install -r requirements.txt"
  }
}
